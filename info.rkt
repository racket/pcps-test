#lang info

(define collection "pcps-test")

(define test-omit-paths '("balle-grav-frot.ss"))

(define test-responsibles '((all mflatt)))

(define deps '("base"
               "gui-lib"
               "scheme-lib"
               "htdp-lib"))
